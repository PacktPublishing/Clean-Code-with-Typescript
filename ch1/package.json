{
  "name": "ch1-typescript-fundamentals",
  "version": "1.0.0",
  "private": true,
  "description": "Chapter 1 examples extracted from the Clean Code with TypeScript manuscript.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
